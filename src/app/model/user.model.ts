export class User {
  public id!: number;
  public name!: string;
  public email!: string;
  public password!: string
  public  mobile!: string;
  public address!: string;  // Assuming address is a string in the form of "Street, City, State, Zip"
  // Add other properties as needed
}

